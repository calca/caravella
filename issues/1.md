## Build Error

The Android CI build is failing with multiple type mismatch errors in `lib/home/home_page.dart`.

### Workflow Details
- **Workflow**: Android CI - Development
- **Job ID**: 62149829420
- **Run URL**: https://github.com/calca/caravella/actions/runs/21570922208/job/62149829420
- **Commit**: 2e9f3d222ef98d8e48391452c58ba8b7d5d6bce8
- **Date**: 2026-02-01

### Error Summary

The build fails with type assignment errors related to pattern matching. The matched value type `({bool isFirstStartFromPrefs, bool shouldShowWelcome})` isn't assignable to the required type `(Object?, Object?)`.

### Affected Lines

**Line 152:**
```dart
final (shouldShowWelcome, isFirstStartFromPrefs) = _shouldShowWelcomeScreen(hasGroups);
```
Error: The matched value of type '({bool isFirstStartFromPrefs, bool shouldShowWelcome})' isn't assignable to the required type '(Object?, Object?)'

**Line 156:**
```dart
if (hasGroups && isFirstStartFromPrefs) {
```
Error: A value of type 'Object?' can't be assigned to a variable of type 'bool'.

**Line 165:**
```dart
final newViewKey = shouldShowWelcome
```
Error: A value of type 'Object?' can't be assigned to a variable of type 'bool'.

**Line 182:**
```dart
_isFirstStart = shouldShowWelcome;
```
Error: A value of type 'Object?' can't be assigned to a variable of type 'bool'.

**Line 227:**
```dart
final (shouldShowWelcome, _) = _shouldShowWelcomeScreen(hasGroups);
```
Error: The matched value of type '({bool isFirstStartFromPrefs, bool shouldShowWelcome})' isn't assignable to the required type '(Object?, Object?)'.

**Line 230:**
```dart
final newViewKey = shouldShowWelcome
```
Error: A value of type 'Object?' can't be assigned to a variable of type 'bool'.

**Line 247:**
```dart
_isFirstStart = shouldShowWelcome;
```
Error: A value of type 'Object?' can't be assigned to a variable of type 'bool'.

### Build Output
```
Target kernel_snapshot_program failed: Exception
FAILURE: Build failed with an exception.
Execution failed for task ':app:compileFlutterBuildStagingRelease'.
Process 'command '/opt/hostedtoolcache/flutter/stable-3.38.9-x64/bin/flutter'' finished with non-zero exit value 1
BUILD FAILED in 8m 11s
```

### Suggested Fix
The `_shouldShowWelcomeScreen` method appears to be returning a named record `({bool isFirstStartFromPrefs, bool shouldShowWelcome})`, but the pattern matching syntax expects a positional record `(Object?, Object?)`. 

Consider either:
1. Changing the return type to a positional record: `(bool, bool)`
2. Or accessing the named fields directly instead of using pattern matching