<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include app data directory for backup -->
    <include domain="shared_prefs" path="." />
    <include domain="database" path="." />
    <include domain="file" path="." />
    
    <!-- Exclude sensitive files if any -->
    <exclude domain="shared_prefs" path="FlutterSecureStorage" />
</full-backup-content>