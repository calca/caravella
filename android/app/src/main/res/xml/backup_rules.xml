<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include app data directory for backup -->
    <include domain="sharedprefs" path="." />
    <include domain="database" path="." />
    <include domain="file" path="." />
    
    <!-- Exclude sensitive files if any -->
    <exclude domain="sharedprefs" path="FlutterSecureStorage" />
</full-backup-content>